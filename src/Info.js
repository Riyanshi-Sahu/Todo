import React from "react";
import Tasks from "./Tasks";



const Info=({show , onClose ,Task})=>{
    const list = useContext(Taskcontext);
    return(
            <>
                
            </>
    );
};
